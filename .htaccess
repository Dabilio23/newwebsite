# Disable directory listing
Options -Indexes

# Default index files
DirectoryIndex index.html index.php

# Protect .env file
<Files ".env">
    Require all denied
</Files>

# Protect hidden files like .gitignore, .env, etc.
<FilesMatch "^\.">
    Require all denied
</FilesMatch>
